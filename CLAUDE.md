# Las Nubes — Notas para Claude

## Preferencias del usuario

- **Auto-merge de PRs**: cuando completes un PR (after push + create), mergéalo directamente sin pedir confirmación. El usuario quiere flujo continuo. Para cambios destructivos o riesgosos (force push, drop tablas, etc.) sigue confirmando.
- **Español neutral latinoamericano** en todos los textos visibles al cliente final (Agente de WhatsApp, plantillas, emails, dashboard). NADA de voseo rioplatense: usar `tú`/`tienes`/`puedes`/`dime`/`toca`/`escríbeme` — NO `vos`/`tenés`/`podés`/`decime`/`tocá`/`escribime`. Aplica a código nuevo y a strings que se reescriban; código viejo con voseo se va migrando a medida que se toque.

## Arquitectura

- **Frontend**: `dashboard.html` (single-file SPA), servido vía GitHub Pages desde `main`.
- **Backend**: Google Apps Script. El código vive en `apps-script/` y se auto-deploya al mergear a `main` (workflow `.github/workflows/deploy-gas.yml`).
- **Bootstrap del Apps Script**: workflow manual `pull-gas.yml` baja el código actual del Apps Script al repo en una rama `bot/pull-gas-<run_id>`.
- **Web App URL**: hardcodeada en `dashboard.html` como `SHEETS_API_URL`.

## Despliegue

- Cualquier push a `main` que toque `apps-script/**` dispara `Deploy Google Apps Script`. Sube los archivos, crea versión nueva y actualiza el deployment de producción.
- **Límite de 200 versiones**: Apps Script topa en 200 versiones por proyecto y no se borran por API (solo a mano en el editor). Si se llena, el deploy falla con `RESOURCE_EXHAUSTED`; el usuario limpia versiones viejas desde el editor y re-corre el deploy.
- Después de cambios de schema (columnas nuevas en Sheets), recordar al usuario ejecutar manualmente la función de migración correspondiente desde el editor de Apps Script.

## Malaya Lodge (cabaña referida)

- **No es parte del inventario de Las Nubes**: es una cabaña del cliente Celestino (+507 6542-9927 / malayalodge@gmail.com). Yo cierro reservas como referido y cobro comisión ($10 dom-jue / $20 vie-sáb por noche).
- **Landing público**: `malaya.html` (servido por GitHub Pages como `/malaya.html`). Calendario standalone, no se conecta al sistema de Las Nubes.
- **Sync iCal bidireccional**:
  - **Airbnb → Las Nubes** (pull): trigger `syncMalayaAirbnb` cada 30 min lee el `.ics` público de Airbnb (URL en Script Property `MALAYA_AIRBNB_ICAL`) y refleja los bloqueos en una hoja interna `MalayaIcal`. Cross-checkea las reservas directas pendientes: si pasaron > `MALAYA_GRACE_MINUTES` (default 60) sin que Celestino bloquee en Airbnb → estado `no_bloqueada` y alerta WhatsApp/email al admin.
  - **Las Nubes → Airbnb** (push, vía polling de Airbnb): endpoint público `doGet?action=malayaIcal` en el Web App expone las directas activas como `.ics`. Celestino importa esa URL en Airbnb → Sincronizar calendarios; Airbnb la polla cada pocas horas y bloquea las noches automáticamente. Función en Malaya.gs: `getMalayaIcalFeed()`. Test desde editor: `_testMalayaIcalFeed()`.
- **Hoja `Malaya`**: 14 columnas (id, huésped, teléfono, checkin, checkout, noches, personas, monto total, comisión, origen, estado, airbnb_blocked, fecha_reserva, notas). Estados: `pendiente | confirmada | no_bloqueada | cancelada | completada`.
- **Setup inicial** (correr una vez en el editor): setear Script Properties `MALAYA_AIRBNB_ICAL`, `MALAYA_CELESTINO_PHONE`, `MALAYA_GRACE_MINUTES`, después correr `instalarTriggersMalaya()`.
- **Notificación WA a Celestino con redundancia**: la plantilla `malaya_reserva_celestino` se envía a Celestino y opcionalmente a más contactos (Glorimar, etc.) para reducir el riesgo de que el aviso pase desapercibido. Los extras se configuran en Script Property `MALAYA_EXTRA_NOTIFY_PHONES` como CSV (E.164 sin `+`). Default en código: `50761000079` (Glorimar). El fallback forward-manual al admin sigue disparándose sólo si ningún destinatario recibe la plantilla.
- **Flujo operacional**: admin entra a `malaya.html?admin=1`, selecciona rango en calendario, sube voucher (parseado con Claude, mismo que Las Nubes), ingresa WhatsApp del huésped, click "Bloquear reserva". Tras eso, avisa manualmente por WhatsApp a Celestino y al huésped. Celestino bloquea en Airbnb. Sync verifica. Si Celestino no bloquea → alerta automática.

## Stack relevante

- Hoja `Reservas`: 33 columnas. La #32 es `VouchersMeta` (desglose JSON pago por pago — ver "Multi-voucher por reserva"). La #33 es `Regalo` (certificado de regalo — ver sección propia). La #25 es `Tipo` (noche/pasadia/pasadia-largo/early/late, agregada en `migrarColumnasV4`). La #26 es `VoucherURL` (link al voucher en Drive, persistido por `saveVoucherToDrive`). La #27 es `IdHuespedURL`. La #28 es `FechaNacimiento`. La #29 es `CheckoutExtendido` (boolean, cortesía 12:30pm). Las #30 (`HoraEntrada`) y #31 (`HoraSalida`) son overrides horarios opcionales por reserva en formato `HH:MM` (24h); vacío = usa el default del tipo. Cuando están seteados, pisan tanto el default del tipo como `CheckoutExtendido`. **Guard un-solo-día**: en `pasatarde`/`pasadia` entrada y salida caen el mismo día. Si al guardar el admin pone un `HoraSalida` custom ≤ la entrada, `saveReservation` **convierte la reserva a `noche`** (hospedaje regular): interpreta que esa salida aplica al día siguiente (checkin=día real, checkout=día+1), conserva la hora de entrada del tipo original como `HoraEntrada` (12:30pm pasatarde / 9am pasadía) y mantiene la salida custom como check-out temprano. Como red de seguridad para datos legacy aún sin re-guardar, la superficie de display **ignora** una salida inválida en pasatarde/pasadía (usa el default del tipo): ocupación (`_reservationOccupancySpans` — ojo: ahí `salidaDay` debe ser el MISMO día que `entradaDay` en `pasatarde`/`pasadia`; el storage de `pasadia` es `ci = día−1, co = día+1`, así que caer al default `co` dibujaba el bloque cruzando la noche), detalle (`showDispDetail`), email/página pública (`tipoEmailMeta`) y WhatsApp/recordatorios (`_horaPlantilla`). `getOrCreateSheet` auto-asegura todas las columnas en cada llamada. **IMPORTANTE — al leer filas**: cualquier lector que construya un objeto reserva y muestre horas debe incluir `horaEntrada: _normalizeHora(r[29])` y `horaSalida: _normalizeHora(r[30])`, o mostrará el default del tipo. Lectores que ya las incluyen: `getReservations` (Parser), `_readReservaById` (PublicLink, página pública), `enviarRecordatoriosCheckin` + `_adminGetMovimientosDia` (recordatorios) y los readers de `BotConsultor`. La función mirror `_botConfirmacionText` usa `_horaPlantilla` para las horas (no hardcodear como hacía antes).
- Hoja `Egresos`: 12 columnas (ID, Fecha, Descripcion, Monto, Categoria, Cabaña, Proveedor, URLFoto, `Item`, `FechaFin`, `MontosItem`, `CantidadesItem`). `_getEgresoSheetEnsured` (Parser.gs) auto-migra hojas viejas agregando `Item`, `FechaFin`, `MontosItem` y `CantidadesItem` al final. `CantidadesItem` (#12) es un JSON `{ keyword_normalizada: cantidad }` paralelo a `MontosItem`: cuántas unidades de cada item trae la factura (ej. 3 garrafones). **En el repartidor el campo de dinero es el PRECIO UNITARIO**: el editor multiplica `cantidad × precio unitario` y guarda el TOTAL en `MontosItem` (`_sumLineTotal`), la cantidad en `CantidadesItem`, y muestra "= $total" en vivo. Al recargar, el precio unitario se deriva (`_sumUnitPrice` = total / cantidad). Cantidad vacía = 1 unidad. Opcional; en Detalle se ve precio unitario y unidades acumuladas por item. NO afecta la contabilidad general. `FechaFin` (#10) es la fecha en que "se terminó" un insumo (editable desde el timeline de Suministros); vacío = se asume el día antes de la próxima compra. `MontosItem` (#11) es un JSON `{ keyword_normalizada: monto }` que reparte una factura entre varios items (ej. un voucher de gas+agua): permite fijar cuánto de la factura corresponde a cada keyword en el timeline. Vacío = cada item usa el `Monto` total. NO modifica el `Monto` global del egreso (contabilidad sigue igual). `Item` (#9) quedó como campo legacy del enfoque anterior — ya no lo usa el timeline.
- **Sub-tab Suministros** (Contabilidad): timeline horizontal tipo Gantt. El usuario define **keywords** (agua, gas, papel…) guardados en la hoja `SuministrosItems` (col 1 `Keyword`, col 2 `NoTimeline` bool; acciones `getSuministrosItems`/`saveSuministrosItems` devuelven/reciben `keywords` + `noTimeline`; semilla inicial de sugeridos). Una keyword marcada **"solo detalle"** (`NoTimeline=true`, ej. Delivery — no es un insumo consumible) se **excluye del timeline** pero sí aparece en la vista Detalle como sección aparte para ver el gasto acumulado. Toggle por chip (`toggleSumNoTimeline`, icono 📊/📋), estado en `_sumNoTimeline` (Set normalizado), helper `_sumIsNoTimeline(kw)`; el timeline se renderiza con `kws.filter(kw => !_sumIsNoTimeline(kw))`. El tracker escanea egresos de categoría **`Suministros` Y `Servicios`** (`_SUM_CATS`/`_sumCatOk(e)`): Suministros = insumos consumibles; Servicios = servicios recurrentes (recolección de basura, delivery, mensajería). La basura recolección va en `Servicios` (es un servicio, no un insumo) y por eso aparece en el tracker. Por cada keyword se buscan los egresos de esas categorías que **pertenezcan a ese item** — regla `_sumMatchesKw(e, nkw)`: la descripción contiene la palabra (substring sin acentos) **O** el egreso tiene un monto asignado a ese item en `MontosItem` (permite agregar un item al reparto aunque no esté en la descripción, ej. una escoba olvidada). **Descartar de un item**: si un keyword agarra una factura por error (ej. "café" en "cafetería reunión"), se marca `MontosItem[nkw] = false` (sentinel) → `_sumMatchesKw` la excluye del timeline/detalle/totales de ese item (no borra el egreso ni toca la contabilidad). En la fila del Detalle hay un ✕ (`_sumDescartarDeKw`); las descartadas se listan al pie de la sección con "↺ restaurar" (`_sumRestaurarEnKw`, borra el flag false). Se dibuja una barra de consumo por compra: fin = `FechaFin` si está seteada, sino el día antes de la próxima compra; la última barra llega a hoy y su color es un semáforo de recompra (verde `#6a9e62` = normal; chocolate `#c0956a` = `dias ≥ 0.8*avg`; rojo `#c0392b` = `dias > avg`); las barras anteriores siempre van verdes. Debajo del timeline hay una **leyenda de esos colores** (`#sumTimelineLegend`, visible solo en la vista Timeline). Al **clickear la barra o el punto** se abre un modal (`openSumEgresoModal`) con: detalles de la factura, link externo a Drive (voucher), **repartidor de montos** y **fecha en que se acabó** editable (persiste en `FechaFin`), con botón "Automático" para borrar el override de fecha. **Repartidor** (`_buildSumSplitEditor`): detecta TODOS los items cuya keyword matchea la descripción del egreso; si son ≥2 (factura compartida, ej. un Yappy de gas+agua) muestra un campo de monto por item con reparto equitativo del resto entre los no editados (`_sumSplitOnInput`/`_sumSplitRebalance`: al fijar uno, los demás no-tocados se rebalancean; línea de conciliación "Asignado $X / $total ✓ cuadra"). Si es 1 item, un campo simple. Persiste todo en `MontosItem`. **Sugerencias de items sin trackear** (`_sumSuggestUntracked`/`_sumSuggestBlockHtml`): analiza la descripción y ofrece como chips las palabras que podrían ser items nuevos (no trackeados), filtrando conectores/unidades/envases (`_SUM_STOP`) y lo ya trackeado. Al clickear un chip (`_sumAddSuggestedKeyword`) se agrega la keyword, se reconstruye el editor preservando lo tecleado (via `overrides` = `{value: TOTAL, qty, touched}`, no el precio unitario crudo) y el item nuevo toma el saldo no asignado. El modal también tiene un input libre **"+ agregar item"** (`_sumModalAddItemHtml`/`_sumModalAddItemInput` → reusa `_sumAddSuggestedKeyword`) para sumar un item que NO está en la descripción (ej. un delivery); `_buildSumSplitEditor` incluye en `matched` las keywords presentes en `overrides` aunque no matcheen por descripción/monto. Útil cuando la conciliación marca "faltan $X". Ojo: guardar el reparto NO convierte una fecha automática en manual — `fechaFin` solo se persiste si el usuario cambió el input respecto al prefill (`_sumEgresoModalCtx.fechaFinPrefill`). El subtítulo de cada fila muestra `N compras · ~Xd · $total` (total = suma de los montos por item). Todo persiste vía `updateEgreso`. Un mismo egreso puede aparecer en varios items, así que `_sumBarMeta` se indexa por `barKey` (`sb_<nkw>_<id>`), no por egresoId. **Dos vistas** con toggle (`setSumView`, estado `_sumView`): **Timeline** (`_renderSumTimeline`; **columna Item bajo el keyword** = total acumulado del período `N compras · [qty] uds · ~Xd · $total`; **debajo de cada barra** su propia leyenda `[✓] [qty] × $monto (Xd)` —los `Xd` son los días que duró esa barra (o los que lleva durando la última, `_daysBetween(p.date, end)`)— con la porción de ESA compra (cantidad siempre, default 1 si no hay); el **ganchito ✓** (verde) marca facturas verificadas vía `_sumEgresoVerificado(e, allNkw)`: las de **un solo item** siempre (nada que verificar), las **compartidas** solo si **cuadran** (`_sumAssignedTotal(e)` == total, mismo criterio que el "Asignado / Total · cuadra" del modal); sin ✓ = esa compartida no cuadra (falta o sobra). **Badge "⚠ Sin cuadrar: $X"** (rojo, esquina superior derecha del sub-tab, en `#sumUncuadradasBadge` sobre el timeline): suma de las **brechas** (no de los montos completos) de las compartidas del período que no cuadran (`_sumListaUncuadradas`). Un descuadre REAL se cuenta **solo cuando TODOS los items matcheados ya tienen porción asignada** (`matched.every(_sumItemAmount != null)`) pero la suma ≠ total (ej. residuo $0.30). **`_sumAssignedTotal(e)` suma solo las porciones de keywords ACTUALMENTE trackeados** (no todo `MontosItem`): así ignora porciones **huérfanas** de keywords borrados (ej. "agua" eliminada que dejó su monto en `MontosItem`) y coincide con el "Asignado $X" del modal —si sumara todo, esas huérfanas darían "exceso" fantasma mientras el modal muestra "faltan". Si algún item falta por asignar, el modal reparte el sobrante entre los que faltan → cuadra: eso es **reparto pendiente**, NO descuadre, y no cuenta (evita inflar el total con facturas a medio repartir o sin repartir). Clic → lightbox `#sumUncuadradasOverlay` (`openSumUncuadradas`) que lista cada factura con su brecha (`faltan $X`/`exceso $X`), proveedor, fecha y asignado/total, y al tocarla abre su reparto (`_sumOpenReparto` → `openSumEgresoModal`); `rowH`/`barTop` dan el alto para que quepan barra + leyenda (una sola línea por row; las compras del mismo item comparten la misma línea) y **Detalle por item** (`_renderSumDetalle`) que lista, por keyword, la tabla de facturas asociadas (fecha, descripción, proveedor, **cantidad + precio unitario**, monto de la porción, duración, voucher) con el total por item, el total de unidades en el subtítulo y un "Total trackeado" global (suma de porciones). La **cantidad por item** (opcional) se edita con un campo "cant." junto al monto en el panel de compartidas y en el modal (single + multi), persiste en `CantidadesItem` (helpers `_sumParseCantidades`/`_sumItemQty`, `_persistSumEgreso` acepta `qtyOps`). Ambas vistas pueblan `_sumBarMeta`, así que las filas del Detalle abren el mismo modal de edición. **Ambas vistas respetan el filtro de período de Contabilidad** (`_matchesSelectedPeriod(e.date)` en los scans de egresos): por default muestran "Este mes"; para ver un rango largo, usar Custom. **La vista Detalle agrupa en 2 secciones marcadas** (encabezados con divisor, `_secHead`): **📦 Suministros** (keywords normales) y **🧾 Servicios** (las keywords "solo detalle"/NoTimeline — servicios como basura, delivery, etc.). Los items NoTimeline **siempre se ordenan al fondo** (`kws.slice().sort(...)` estable por `_sumIsNoTimeline`), sin importar el orden en que se agregaron; cada grupo se arma con `renderKwCard(kw)` (extraído del forEach anterior; acumula `granTotal` por closure). Dentro de **Suministros**, arriba de las tarjetas, va el **panel "📌 Facturas compartidas"** (colapsable, **cerrado por defecto** — `_sumSharedOpen`/`toggleSumShared`, el encabezado muestra el conteo y un badge "⚠ N sin repartir"; el toggle no re-renderiza) con cada voucher cuya descripción matchea ≥2 keywords: inputs de monto por item + reparto equitativo del resto (`_sumSharedOnInput`/`_sumSharedRebalance`), conciliación en vivo (`_sumSharedRecon`), botón "⚖️ Equitativo" (`_sumSharedRepartirIgual`), chip de estado (✓ repartida / ⚠ sin repartir) y un input **"+ agregar item"** (`_sumSharedAddItemInput`/`_sumSharedAddItem`) para sumar al reparto un item que no está en la descripción (crea la keyword si no existe, lo agrega en $0 y persiste). Persiste al salir del campo (`_sumSharedOnChange` → `_persistSumEgreso`, que re-renderiza). Estado en `_sumSharedState` (por egresoId, soporta varios vouchers en pantalla a la vez, a diferencia del modal que usa `_sumSplitState` global). **Widgets superiores** (`_renderSumTopWidgets`, contenedor `#sumTopWidgets`, arriba del sub-tab): dos tarjetas "Mayores gastos" — **📦 Suministros** y **🧾 Servicios** — con el top-5 de keywords por gasto del período (barra proporcional) y el total del grupo; respetan el filtro de período. Los **keywords (chips `#sumKeywordChips` + input) y la explicación** viven dentro de un `<details>` colapsable **"🔑 Palabras clave (N)"** (cerrado por defecto, sutil; el conteo N se actualiza en `renderSuministrosTab` vía `#sumKwCount`). Helpers frontend: `renderSuministrosTab` / `_renderSumTopWidgets` / `setSumView` / `_renderSumTimeline` / `_renderSumDetalle` / `_sumItemAmount` / `_sumParseMontos` / `_buildSumSplitEditor` / `_sumSplitRebalance` / `_sumSharedRebalance` / `openSumEgresoModal` / `saveSumEgresoModal` / `_persistSumEgreso`.
- **Dedup de egresos**: al guardar (`saveEgresos`) se rechaza un duplicado (status `duplicate`) salvo que se mande `force:true`. Detecta: (1) **código de referencia** `[XXXX-YYYY]`/`[000000…]` ya presente (`_egresoRefCodes`); (2) **huella de canasta** (`_egresoBasketSig` = multiset de `descNorm|monto`, sin proveedor ni fecha, solo si ≥2 items) → atrapa la misma factura re-subida con otra fecha o grafía de proveedor. `_egresoBatchDuplicate` en Parser.gs; espejo en frontend (`_egBasketSig`/`_egRefCodes` en `saveEgreso`) que avisa antes con confirm y manda `force` si el usuario acepta. La huella de 1 solo item NO se usa (para no bloquear pagos recurrentes tipo "limpieza $150"); esos siguen con el fallback misma-fecha+proveedor+total. Script puntual de limpieza de duplicados ya detectados: `apps-script/Cleanup.gs` (`previewEgresosDuplicados()` para revisar, `borrarEgresosDuplicados()` para borrar).
- Modelo IA: `claude-opus-4-6` para OCR de vouchers/facturas (en `parseVoucherWithClaude` y `parseFacturaEgresoConClaude`). `parseVoucherWithClaude` tiene retry con fallback a Sonnet 4.6.
- API key Anthropic: en Script Properties (`CLAUDE_API_KEY`), nunca en código cliente.

- **Mini-timeline de ocupación del modal** (`renderMiniTimeline`): ventana **dinámica**, no días completos. Arranca a las **6am del día ancla** y el fin es `max()` de: (a) **mínimo** día siguiente a las **6pm** (`MIN_TO_H=18` — cubre salida, limpieza, entrada de 2pm y un late check-out de 4pm), (b) **check-out real + `LIMPIEZA_H=3`** (para estadías de 2, 3 o más noches), (c) **`GLIMPSE_H=2`** de la próxima reserva si empalma dentro de `EMPALME_H=12`. El rango se parte en tramos por día (`days`/`winStart`/`winEnd`/`winH`/`winTop`, clampeados al rango); como los tramos son contiguos en el tiempo, `dtToPx` es lineal. `HOUR_H` se adapta (`MAX_TOTAL_PX=1600`, piso 7px/h): hasta ~4 noches mantiene los 14px/h y solo estadías largas se comprimen — el contenedor scrollea. `BOTTOM_PAD=46` da aire al último item (un "X entra" de 2 líneas cae sobre el borde). El item de **ventana de limpieza se omite** si el hueco comprimido no le da (`maxTop < prevBottom - 2`): la info queda implícita entre el "sale" y el "entra". Si la próxima reserva queda **fuera** de la ventana, se anuncia en un pie `.mt-next` ("→ Próxima reserva: …", clickeable); si no hay ninguna, un pie tenue lo dice.

## Modelo de datos para tipos de reserva

Cada reserva se almacena con un rango de bloqueo (checkin/checkout) y un `tipo`. El frontend tiene dos representaciones:

- **Storage** (lo que está en la hoja): rango de días bloqueados.
- **Form / display** (lo que ve el usuario): día real del huésped.

| Tipo            | Storage `checkin`    | Storage `checkout`    | Form `entrada`    | Form `salida`     |
|-----------------|----------------------|-----------------------|-------------------|-------------------|
| `noche`         | día llegada          | día salida            | igual             | igual             |
| `pasadia`       | día pasadía          | día pasadía + 1       | día pasadía       | día pasadía       |
| `pasadia-largo` | día pasadía − 1 (cortesía) | día pasadía + 1 | día pasadía       | día pasadía       |
| `early`         | día llegada − 1 (cortesía) | día salida real | día llegada (9am) | día salida (11am) |
| `late`          | día llegada          | día salida + 1 (cortesía) | día llegada (2pm) | día salida (4pm)  |

Helpers en `dashboard.html`:
- `_formFromStored(r)` y `_storedFromForm(entrada, salida, tipo)`: traducen entre representaciones.
- `reservaDisplayDates(r)`: para mostrar en tablas/popups (devuelve `displayCheckin`, `displayCheckout`).
- `reservaNochesReales(r)`: 0 para pasadías, 1 para early/late, N para noche.
- `reservaDuracionLabel(r)`: "Pasadía" / "1 noche" / "N noches" / etc.
- `getDayKind(r, dateStr)`: `'main'` o `'courtesy'`.

Helpers equivalentes en backend (`Parser.gs`):
- `tipoEmailMeta(r)`: para emails de confirmación/actualización.
- `buildGuiaHTML(cabin, tipo)`: ajusta hora de check-out en la guía según tipo.

## Notas operativas

- **Fechas de reservas Airbnb (`parseFecha(str, baseISO)`)**: el año de la estadía se deduce de la **fecha del email** (`msgDate`), no de "hoy", con la regla *la estadía es siempre posterior o igual a la fecha de reserva* (`_anioParaEstadia_`). El método viejo (`getCurrentYear`, que compara contra el mes actual) queda solo como fallback: producía años erróneos si el sync corría tarde o se reprocesaba un email viejo — re-parsear en julio una estadía de marzo devolvía el año siguiente (5/13 aciertos vs 13/13 del nuevo). Al tocar `parseAirbnbEmail`, pasar SIEMPRE `msgDate` a `parseFecha`.
- **Ventana de sync Airbnb**: `syncAirbnbReservations`/`syncAirbnbUpdates` buscan `newer_than:365d` (antes 90d). Si una reserva no se registra dentro de la ventana, su email queda fuera de rango y **nunca se reintenta**. Red de seguridad: `reconciliarReservasAirbnb([insertar])` en Cleanup.gs compara TODOS los emails contra la hoja (ignorando Blacklist) y reporta/carga los faltantes sin depender de la ventana.
- **Pagos Airbnb (`syncAirbnbPayouts` → hoja `Pagos`)**: cada payout trae varias líneas `HMXXXXXXXX` con su monto. Airbnb también emite **"Ajuste de la resolución"** (reembolso, monto NEGATIVO) y **"Cobro de la resolución"** (positivo) sobre un código ya cobrado. Los montos se parsean con `_montoUSDConSigno_(linea)` que **respeta el signo** (`-$99,00`, `$-99,00`, `($99,00)`) y se **acumulan** por código, así el neto queda correcto. Ojo histórico: el regex viejo solo capturaba dígitos, así que un ajuste de −$99 entraba como +$99 y el código quedaba sobrecontado por el DOBLE del ajuste (HMWC8N44BR: real $68.31 → $266.31, descuadrando ese payout en $198). Relación esperada por payout: **`suma_códigos − ComisionWU($10) = MontoNeto`**.
- **Sync Pagos → Reservas (`actualizarEstadoPagoAirbnb`)**: Airbnb puede pagar UNA reserva en **varios payouts** (estadías largas). `pagoMap` suma los parciales, pero el guard de salida solo miraba `estadoPago === 'PAGA'`, así que al llegar el 2º cobro la fila ya estaba PAGA y `MontoPagado` se quedaba con el parcial (LARS $47.10 de $271.24; GORAN $84.60 de $213.45). Ahora también compara el monto: solo saltea si `|MontoPagado − pago.montoPagado| < 0.01`.
- **Multi-cabaña (`_saveMultiCabinReservation`)**: crea N reservas "hermanas" (una por cabaña). El **monto** se prorratea por noche (`perNight`) y el **voucher se reparte en la MISMA proporción** — nunca se copia completo en cada hermana. El `deposit` va solo en la primera. Helper `_fix(arr, target)` hace que el último grupo absorba el redondeo, así la suma de montos == total y la suma de vouchers == monto del voucher. **Ojo histórico**: antes el voucher completo se escribía en cada hermana, así que un pago único de $150 en 2 cabañas se leía como $300 cobrados (inflaba "recibido" en Ingresos y tapaba saldos pendientes). Reservas multi-cabaña creadas antes de ese fix necesitan corregirse a mano en la hoja.
- **Widgets de Ingresos** (`_renderWidgetContent`): el número grande es lo **recibido** (`montoRecibido`), no el monto facturado. El subtítulo tiene 3 estados: `por cobrar $X` (suma de faltantes **reserva por reserva** vía `porCobrar(arr)` — NO netea contra sobrepagos de otras, para que un voucher inflado no tape una reserva sin cobrar), `⚠ sin monto registrado` (hay reservas facturables pero el monto total es 0, ej. Airbnb sin importar montos) y `✓ cobrado`. La **tendencia** compara neto vs neto contra un período del mismo largo inmediatamente anterior (para un mes = el mes anterior) y se rotula "vs período anterior".
- **Mix de producto / desglose por tipo** (`renderContabilidad`): `tipoStats`/`tipoIngresos` se acumulan **dinámicamente** (`tipoStats[t] = (tipoStats[t]||0)+1`) y el grid itera sobre los tipos **presentes**, con fallback de label/color para tipos desconocidos. Antes era un dict fijo `{noche,pasatarde,pasadia,early,late}` y el guard `!== undefined` descartaba en silencio tipos como `pasanoche` (quedaban fuera del desglose aunque su monto sí sumaba al total). Al agregar un tipo nuevo, basta con sumarlo a `tipoLabels`/`tipoMeta` para que tenga label y color propios.
- **Filtro de período de Contabilidad** (`renderContabilidad` → `#monthFilter`): primero **`YTD`** (año a la fecha: 1-ene del año en curso → hoy, `setPeriod('ytd')` → `selectedRange` con `presetKey:'ytd'`), luego un chip por cada mes desde **enero del año en curso hasta "Este mes"** (mes actual, rotulado así y tinteado), un divisor y **`Custom…`** para cualquier otro rango (incluye meses de años anteriores). Estado en `selectedMonth` / `selectedRange`, precedencia en `_matchesSelectedPeriod(iso)`; **default = mes actual** ("Este mes"). `setPeriod('YYYY-MM' | 'ytd' | 'customToggle' | 'thisMonth' | 'lastMonth' | 'last3' | 'last6' | 'todo')` sigue soportando todos los presets aunque la barra ya no renderice chips para `Todo`/`Últimos N`/`Mes anterior`. El filtro es **global**: lo respetan Resumen, Ingresos, Egresos, Por Cobrar y Suministros.
- **Columna `Alerta` (14) — NO es solo de Airbnb**: acumula notas de tres orígenes separadas por `" | "`: `⚠ Verificar` (`syncAirbnbUpdates`, Airbnb notificó un cambio), `❌ Cancelado <fecha>` (cancelación de Airbnb) y `📅 Entrada: … | Salida: …` (**cambio de fechas hecho por el admin desde el dashboard, en CUALQUIER origen** — lo escribe `saveReservation` cuando llega `payload.fechaAnterior`). El detalle la renderiza con `_renderDetailAlerta(r)`, que decide por **contenido**: antes mostraba el aviso de Airbnb hardcodeado con solo ver la celda no vacía, así que a una Directa con fechas editadas le salía "Airbnb notificó un cambio". El cambio de fechas se muestra en tono neutro (es historial, no alerta) y siempre se imprime el texto real de la celda. **Ojo**: la nota de fechas lleva un `|` adentro, así que no se puede partir el string por el separador — hay que detectar por marcadores. El pintado naranja del calendario (`.alerta-mod`) ya estaba gateado por `origin === 'Airbnb'`.
- **Origen `'Cortesia'`** (sin tilde) — toda referencia debe ser sin tilde. La forma con tilde es solo el label visible en UI.
- **`SIN_PAGO_ORIGINS`**: constante global en dashboard.html con `['Cortesia','Colaboracion','Personal','Mantenimiento','Abierta']`.
- **Origen `'Mantenimiento'`**: bloqueo interno por trabajo de mantenimiento. En el calendario sale 🔧 en vez de iniciales y la celda usa fondo gris (`.disp-day.occupied.maintenance`).
- **Recibo PDF**: generado por `generateReceiptPDF(r)` en backend. Numeración correlativa `LN-NNNN` en Script Properties (`RECEIPT_COUNTER`). Adjuntado al email de confirmación si la reserva tiene voucher. `sendUpdateEmail` también lo adjunta cuando la edición incluye un voucher nuevo.
- **Vouchers en Drive**: subidos por `saveVoucherToDrive` a la carpeta `Las Nubes - Pagos`. URL persistido en columna 26.
- **Vista previa de la confirmación** (botón "👁 Vista previa" en el modal de reserva): envía la MISMA confirmación (email + WhatsApp) al admin, no al cliente, para revisar cómo le llegará antes de mandarla. Email → `REPLY_TO_EMAIL`. WhatsApp → celular personal del admin en Script Property `PREVIEW_NOTIFY_PHONE` (default `50769812266` = 6981-2266). OJO: NO usar el número emisor del bot (WhatsApp Cloud API registrado en `WA_PHONE_NUMBER_ID`, que es 6018-8278) como destino — Meta no entrega un mensaje del número emisor a sí mismo. Acción backend `previewConfirmacionSelf` (clona la reserva con los contactos del admin y llama `sendConfirmationEmail` —con prefijo de asunto "👁 Vista previa · "— y `sendWAReservaConfirmada`). Frontend: `_buildFormReservation()` arma la reserva desde el formulario (incluye la conversión un-solo-día→noche) y `previewConfirmacionAMi()` hace el POST. Funciona antes de guardar y no adjunta recibo PDF.
  - **`id: 'preview'`** — en una reserva sin guardar `_buildFormReservation()` manda ese id placeholder. Se firma perfecto pero **no existe como fila**, así que un link armado con él abre "Link no válido". Por eso las plantillas (email y WhatsApp) deben usar **`getPublicReservaUrlSafe(id)`** (PublicLink.gs), que devuelve `''` cuando `_esIdPreview(id)` — y ahí cada plantilla omite el botón (WhatsApp cae a `https://lasnubes.cloud`, que no puede ir vacío por ser param de la plantilla). **No usar `getPublicReservaUrl()` crudo en emails ni plantillas.** Ya migrados: `buildEmailHTML`, `buildEmailHTMLAbierta`, `buildEmailHTMLRegalo`, `buildUpdateEmailHTML`, `sendWAReservaConfirmada` y `sendWARegaloCertificado`. Los usos en `BotConsultor` operan sobre reservas ya guardadas, así que quedan crudos.
- **Recordatorio de check-in** (email automático 1 día antes): trigger `enviarRecordatoriosCheckin` corre diario a las 10am Panamá. Escanea Reservas y manda email a quienes tienen `displayCheckin === mañana` (excluyendo CANCELADA, origen Abierta, sin email). Para activarlo, correr una vez desde el editor: `instalarTriggerRecordatorios()`. Para preview: `enviarRecordatorioPrueba()` (envía al email del usuario que corre el script). Configuración (indicaciones, maps URL) en Script Properties: `CHECKIN_MAPS_URL`, `CHECKIN_WAZE_URL`, `CHECKIN_INDICACIONES`, `CHECKIN_ACCESO_EXTRA`.
- **Multi-voucher por reserva** (abonos parciales): cuando se sube un segundo voucher en edición, el sistema acumula:
  - `montoVoucher`: suma total ($45 + $45 = $90).
  - `codTransferencia`: lista separada por `|` (ej. `UYAFL-111|UYAFL-222`).
  - col 26 `VoucherURL`: lista separada por `|` (cada subida agrega).
  - `deposit`: si ya hay abono previo y se sube otro voucher, `handleVoucherUpload` lo suma al campo en lugar de sobrescribir.
  - col 32 `VouchersMeta`: **desglose pago por pago** (el resto de los campos son acumulados). JSON array `[{monto, cod, fecha, url}]`, un entry por voucher, en el MISMO orden que las URLs de la col 26. Se escribe **solo** en `_appendVoucherMeta` (llamado desde `saveVoucherToDrive`) para que ambas listas no se puedan desalinear; el frontend manda el desglose en `payload.voucherMeta`. En multi-cabaña el voucher se sube una sola vez, en la fila de la primera hermana, y su `monto` es la **porción prorrateada** de esa cabaña (las hermanas quedan sin meta → caen al fallback).
  El botón "Ver voucher" del detalle abre cada URL en pestaña separada si hay múltiples. `deleteReservation` con `deleteVoucher=true` itera todas las URLs.
- **Lista de pagos registrados en el modal de reserva** (`#vouchersRegistrados`, debajo del dropzone): una fila por voucher ya asociado — `#N · $monto · código · fecha · Ver ↗` — más un encabezado `Recibido $X de $Y` y un pie con el saldo (`Falta $X` / `✓ Pagado completo` / `Excede $X`). Sirve para ver qué pagos ya entraron antes de subir el que falte. Helpers: `_voucherEntries(r)` (normaliza) y `renderVouchersRegistrados(r)` (pinta); se llama desde `editReservation` (con la reserva) y `openModal` (con `null`, para limpiar). **No** se limpia en `clearVoucher()` — el ✕ del voucher pendiente no debe borrar el historial. Solo se muestra en orígenes `Directa`/`Referido`.
  - **Voucher recién subido**: `handleVoucherUpload` re-pinta la lista al terminar, agregando el pendiente como fila **"por guardar"** (fondo ámbar) y proyectando `Recibido`/`Saldo al guardar` con ese monto incluido — así se ve cómo va a quedar la reserva ANTES de guardar. El total sale del formulario (`#fAmount`), no de `r.amount`, porque el admin pudo cambiarlo o el voucher pudo subirlo. `clearVoucher()` re-pinta para revertir la proyección (solo si la caja ya estaba visible: en `editReservation`/`openModal` corre ANTES del primer render). Contexto en `_vouchersRegCtx`.
  - **Después de guardar** (el modal queda abierto en estado post-save): el contexto de la caja se guarda como **ID** (`_vouchersRegCtxId` + `_rerenderVouchersReg()`), no como referencia al objeto — `saveReservation` reemplaza la entrada de `reservations` por un objeto nuevo, así que una referencia guardada dejaría la caja con los montos de antes de guardar. Además `_appendVoucherMetaLocal(id, entry, entriesPrevias)` espeja localmente lo que `_appendVoucherMeta` escribió en la col 32 (más el `voucherURL`), o el encabezado mostraría el acumulado nuevo con el desglose viejo. Se siembra con `_voucherEntriesPrevias` — foto tomada **antes** de que la edición acumule `codTransferencia`/`montoVoucher` — porque leerlo después haría aparecer el código recién acumulado como un pago fantasma y volvería desconocido el monto del pago viejo; además se deduplica por `cod` sin `url`.
  - **Orden del DOM**: `dropzone → voucherCard → vouchersRegistrados`. Al subir un voucher el dropzone se oculta y el card ocupa su lugar; si la lista fuera antes que el card, saltaría hacia arriba todo el alto del dropzone y el card quedaría por debajo de ella.
  - **También en el popup de detalle** (`#detailPagosBox`, `renderDetailPagos(r)`): la misma caja en **solo lectura** — sin fila "por guardar" y con el monto de la reserva guardada, no el del formulario. El HTML lo arma `_buildVouchersRegHTML(r, {pendiente, origen, total})`, compartido por los dos modales. Va donde antes estaba el badge de referidos (info que se consulta a cada rato); el **badge de referidos se movió al fondo**, después del mini-timeline. La clase `vouchers-reg--detalle` le quita el margen negativo superior (ahí no cuelga de un dropzone).
  - **"sin archivo" con código presente**: hay dos causas y `diagnosticarVouchersSinArchivo()` (Cleanup.gs) las separa contra los datos reales. **(A)** el archivo nunca se subió: pasaba con las reservas del **Agente de WhatsApp**, que descargaba la imagen, la mandaba a Claude y guardaba el código pero **descartaba los bytes** — se reconocen porque el código conserva el `#` que el dashboard sí quita. Ya corregido: `_botGuardarVoucherEnDrive()` sube la imagen en el momento del parseo (único punto donde existen los bytes; el contexto de la conversación vive en una hoja y no puede cargar un base64), guarda la URL en el contexto y `_botCreatePreReservation` la escribe en las cols 26 y 32. **(B)** el archivo sí está en Drive pero la URL no quedó en la hoja → recuperable con **`vincularVouchersHuerfanos(dryRun)`**, que matchea por **id de reserva** (`Cód. Pago:` en la descripción) o por huésped+entrada. OJO: el `repoblarVoucherURLs()` viejo matcheaba por *código de transferencia*, que **no** aparece en la descripción — por eso daba falsos negativos. `vincularVouchersHuerfanos` enlaza como máximo tantos archivos como códigos tenga la fila (varias reservas tienen 2-3 archivos por re-subidas; escribirlos todos inflaría el conteo de pagos), prefiere las subidas más recientes y **anexa** cuando la fila ya tiene alguna URL. **Limpieza histórica corrida en jul-2026**: 114 filas recuperadas; quedaron 11 sin archivo real (3 del bot, irrecuperables porque la imagen solo vivió en WhatsApp) y 2 desalineadas sin segundo archivo en Drive (Marilyn Perez, Felix R.). El diagnóstico **ignora las filas sin `codTransferencia`** (~55), así que un voucher suelto en Drive de una de esas no se reporta.
  - **Reservas legacy** (sin `VouchersMeta`): `_voucherEntries` degrada a `VoucherURL` + `codTransferencia`. Ahí los montos individuales nunca se guardaron, así que solo se afirma el monto cuando hay **un único** voucher (= `montoVoucher`); con varios se muestra `$ ?` por fila y una nota explicando que solo existe el acumulado.

- **Reservas de Airbnb duplicadas (sintética + HM real)**: cuando Airbnb notifica un cambio, la fila vieja puede quedar con un código **sintético** (`airbnb_<hash>`, cuando el sync no pudo leer el HM del email) y con el monto anterior. Después una reconciliación como `insertarReservasAirbnbFaltantes()` ve que el HM real "falta" —busca por **código**, no por fechas— e inserta una fila nueva: quedan **dos filas para la misma estadía**, la vieja sin cobrar y con el monto viejo, que es la que aparece en "por cobrar" aunque Airbnb ya pagó. Caso testigo: Yarisel Rangel (fila sintética $149 sin cobrar + fila `HMBBQXQ3QD` $169 cobrada). Detección: `buscarReservasAirbnbDuplicadas()` agrupa por cabaña+entrada+salida y **separa tres casos**: (a) fila sintética + HM real → sobra la sintética; (b) **mismo código HM repetido** → la misma reserva cargada dos veces, ingreso contado DOBLE, hay que borrar una; (c) **códigos HM distintos** → NO es duplicado, son dos reservas reales que chocan porque **una tiene las fechas mal en la hoja** (año equivocado del bug viejo de `parseFecha`, o una modificación de Airbnb no reflejada) — ahí se corrigen fechas, no se borra nada. Para borrar: `FILAS_A_BORRAR` + `borrarFilasReservas()` (dry-run) / `borrarFilasReservasESCRIBIR()`, que borra **de mayor a menor** porque al eliminar una fila las de abajo suben y los números siguientes apuntarían a la fila equivocada. `diagnosticarPagoAirbnb(query)` también lo avisa. **Ojo al escribir estos diagnósticos**: el veredicto tiene que evaluarse **por fila**, no con un flag global de "el pago existe" — si no, la fila sintética hereda el cobro de su hermana y el reporte dice que solo falta correr `actualizarEstadoPagoAirbnb()`, que sobre ella no hace nada.
- **El editor de Apps Script corre las funciones SIN argumentos** (no hay dónde escribirlos). Dos consecuencias: (1) toda función con parámetros necesita un **runner sin parámetros** — al final de `Cleanup.gs` hay una sección "Atajos para el editor" con constantes editables (`DIAG_PAGO_QUERY`, `DIAG_EMAIL_QUERY`, `RECONCILIAR_DESDE`) y sus runners (`diagnosticarPagoAqui`, `diagnosticarEmailAqui`, `reconciliarAirbnbReporte` / `reconciliarAirbnbINSERTAR`); (2) **el default de una función destructiva tiene que ser el seguro** — `vincularVouchersHuerfanos()` tenía `dryRun` sin default, así que correrla desde el botón Run escribía en silencio; ahora sin argumentos hace dry-run y la que escribe es `vincularVouchersHuerfanosESCRIBIR()`.

## Certificados de regalo (Gift Certificate)

Reserva donde **quien paga y quien disfruta son personas distintas**: `pagador` = quien compró el regalo; `name`/`email`/`telefono` = el **beneficiario**, a quien le llegan todos los avisos. El beneficiario **nunca ve plata** (ni tarifa, ni abono, ni saldo, ni recibo).

- **Es un flag ORTOGONAL al `tipo` y al `origen`, no un valor de ninguno de los dos.** Un regalo sigue siendo una `noche`/`pasadia` (necesita horarios) y sigue entrando por `Directa`/`Referido` (necesita registrar el ingreso). Por eso NO se agregó un origen `Regalo` ni un tipo nuevo: se habría perdido el horario o la contabilidad.
- **Storage**: col 33 `Regalo` = JSON `{de, mensaje}`; **vacío = reserva normal**. `de` = nombre que se muestra como remitente (default: `pagador`), `mensaje` = dedicatoria opcional. Helpers backend `_parseRegalo(r)` / `_serializeRegalo(regalo, pagadorFallback)` / `esReservaRegalo(r)`; espejo frontend `_parseRegaloLocal` / `_buildRegaloFromForm`. Todos toleran JSON corrupto, celda vacía, objeto ya parseado y `esRegalo:false` explícito.
- **Regalo SIN fecha**: se combina con origen `Abierta` (que ya oculta las fechas en el modal). El certificado entonces dice "Fechas: A coordinar" y muestra el CTA "💬 Coordinar mis fechas" en vez del link de detalles. La detección es `origin === 'Abierta' || !checkin || !checkout`.
- **Email**: `buildEmailHTMLRegalo(r)`, despachado desde `buildEmailHTML` **antes** del check de `Abierta` (un regalo sin fecha debe recibir el certificado, no el email genérico de reserva abierta). Asunto `🎁 Tienes un regalo en Las Nubes`. Es un **certificado, no la confirmación operativa**: no repite el manual completo ni la Tiendita (tiene precios) — cuando el beneficiario define fechas, el admin manda la confirmación normal, que sí trae guía de acceso. `sendConfirmationEmail` y `sendUpdateEmail` **no adjuntan recibo PDF ni el voucher** cuando es regalo, y `sendUpdateEmail` reenvía el certificado en vez del email de actualización (ese habla de montos).
- **Invitación a coordinar**: el certificado cierra con `Para verificar disponibilidad y coordinar las fechas de tu reserva escríbenos al WhatsApp +507 6981-2266. Estaremos atentos 🙏`. Constantes `REGALO_WA_NUMERO` / `REGALO_COORDINAR_TXT` / `REGALO_DUDAS_TXT` en Parser.gs (fuente única) + helper `_regaloCoordinarHTML(sinFecha)`. Con **fechas ya puestas** se usa `REGALO_DUDAS_TXT` ("Si necesitas ajustar las fechas o tienes cualquier duda…") — pedirle "coordinar las fechas" a quien ya las tiene fijadas sería contradictorio; el cierre 🙏 se mantiene igual en los dos casos.
  - **Emoji en el email**: en el `htmlBody` de Gmail un emoji crudo sale como `??????`, así que en las plantillas los emoji van como **entidad numérica** (`&#128591;` = 🙏, igual que el resto del archivo). Por eso las constantes guardan el texto **sin** el emoji y cada canal lo agrega: `_regaloCoordinarHTML()` pone la entidad, WhatsApp usa el carácter real. Como red de seguridad `buildEmailHTMLRegalo` pasa todo el HTML por **`_emojiAEntidades()`** (convierte cualquier carácter fuera del BMP), lo que cubre también los emoji que el admin escriba en la dedicatoria. El texto del admin además va por **`_escHtmlEmail()`** para que un `<` en la dedicatoria no rompa el layout.
  - **En WhatsApp el texto no se puede inyectar desde el código**: el body de una plantilla HSM vive en Meta. Por eso la invitación está en el texto sugerido de `certificado_regalo` (docstring de `sendWARegaloCertificado`) y hay que copiarla al registrar la plantilla. Mientras se use el **fallback** a `confirmacion_reserva` (body fijo), viaja en el param libre `fechas` vía `REGALO_WA_COORDINAR_CORTO` — en **una sola línea**, porque Meta rechaza params con saltos de línea o tabs.
- **WhatsApp**: `sendWARegaloCertificado(reservation)` usa la plantilla **`certificado_regalo`** (es_PA, params `{nombre, de, cabana, detalle, link}`). **Hay que darla de alta en Meta Business Manager** — el texto sugerido está en el docstring de la función. Mientras no esté aprobada, cae automáticamente a `confirmacion_reserva` metiendo el marco de regalo en los campos de texto libre (`cabana` = `🎁 <cabaña> — regalo de <de>`), también sin montos. Router `sendWAAvisoReserva(r)` elige entre regalo y confirmación normal; lo usan la action `sendWAConfirmacion` y `previewConfirmacionSelf`. Test desde el editor: `testEnviarPlantillaRegalo()`.
- **Regalo CON fecha definida**: el certificado muestra check-in/check-out con sus horas, las noches y el botón al link público; usa `REGALO_DUDAS_TXT` en vez de la invitación a coordinar. **Ojo**: una reserva con fecha alcanza flujos automáticos que la `Abierta` nunca dispara. El **recordatorio de check-in** (`enviarRecordatoriosCheckin` → `buildCheckinReminderHTML`) mostraba "Saldo pendiente: $X" + botón "Coordinar pago": ahora `hasSaldo` se apaga con `esReservaRegalo(r)`, y el reader del trigger incluye `regalo: data[i][32]` + `pagador: data[i][14]` (sin eso el flag nunca llegaba). El WhatsApp `recordator_entrada` no lleva montos, así que no requirió cambios. Los `$` que quedan en el bot y en la página pública son del **programa de referidos** y de la **Tiendita**, no la tarifa.
- **Página pública** (`reserva.html`): `PublicLink.gs` anula `total`/`deposit`/`saldo`/`pagarUrl` cuando es regalo y manda `regalo: {de, mensaje}`; el frontend pinta el banner con `renderRegalo(r)`. Los `$` que quedan son los precios de la **Tiendita** (contenido estático, no la tarifa) — eso es a propósito.
- **Voucher OCR**: el voucher es del **pagador**, así que con el check de regalo encendido `handleVoucherUpload` **solo** llena "Pago" (y "De parte de" si está vacío); no toca Huésped/Email/Teléfono, que son del beneficiario. En una reserva normal el autollenado sigue igual que siempre.
- **UI**: checkbox `#fEsRegalo` + `#fRegaloDe` + `#fRegaloMensaje` en el modal (`toggleRegalo()` muestra/oculta y cambia el label del email a "— del beneficiario"; `_updateRegaloHint()` explica el caso con/sin fecha según el origen). Badge `#detailRegaloWrap` en el detalle. Desmarcar el check manda `regalo: ''` → el backend **borra** la col 33.
